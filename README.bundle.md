# Stage 12 Candidate Promotion Queue Additive Bundle

## Run

```text
task_manifest = tools/tasks/formalism_tests_tasks.stage12.additive.json
task_id       = stage12_candidate_promotion_queue_tests
```

## Active Manifest Integration

After the additive test passes, merge the Stage 12 task into:

```text
tools/tasks/formalism_tests_tasks.json
```
