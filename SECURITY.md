# Security Notes

This harness does not require live secrets, does not call live LLM providers, and does not mutate production repos. Confidence is derived from artifact and receipt alignment, not model self-claims.
